// Path: crates/api/src/commitment/identifiers.rs
//! Defines the unique identifier for a commitment scheme.

// Re-export from the `types` crate to break the circular dependency.
pub use ioi_types::commitment::SchemeIdentifier;
